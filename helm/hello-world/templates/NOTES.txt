{{ .Chart.Name }} {{ .Chart.AppVersion }} installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

To access the Hello World app from your local machine:

  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "hello-world.fullname" . }} 8080:{{ .Values.service.port }}

Then visit http://localhost:8080 in your browser, or:

  curl http://localhost:8080

To view running pods:

  kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}"